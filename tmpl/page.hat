<!doctype html>
<html lang="en">
    <head>
        include("meta", args.meta)
    <body .flex.col .height-screen .margin-vertical-none.margin-horizontal-large .font-segoe .bgcolor-thunder>
        include("header", load("header"))
        <main .grow>
            <div .markdown>
                args.markdown
            if !empty?(args.additions)
                print(args.additions)
        include("footer", load("footer"))